/* Google Fonts Import */

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

/* CSS Variables for Consistent Theming */

:root {
    --primary-blue-dark: #1e3a8a;
    --primary-blue-light: #3b82f6;
    --primary-blue-lighter: #60a5fa;
    --accent-green: #10b981;
    --accent-green-dark: #059669;
    --accent-red: #dc2626;
    --accent-red-dark: #b91c1c;
    --accent-orange: #f59e0b;
    --accent-purple: #8b5cf6;
    --text-light: #f8fafc;
    --text-dark: #1e293b;
    --text-gray: #64748b;
    --background-light: #f1f5f9;
    --background-dark: #0f172a;
    --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    --card-hover-shadow: 0 20px 50px rgba(0, 0, 0, 0.12);
    --transition-fast: 0.2s ease;
    --transition-normal: 0.3s ease;
    --transition-slow: 0.5s ease;
}


/* Reset and Base Styles */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    font-size: 16px;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    overflow-x: hidden;
    background-color: var(--background-light);
    color: var(--text-dark);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    padding-top: 107px;
}


/* ============================================
   TOPBAR (Üst Bilgi Çubuğu)
   ============================================ */

.topbar {
    background: linear-gradient(135deg, var(--primary-blue-dark), var(--primary-blue-light));
    color: var(--text-light);
    padding: 0.5rem 0;
    font-size: 0.875rem;
    box-shadow: 0 2px 20px rgba(30, 58, 138, 0.3);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 999;
}

.topbar-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.topbar-left,
.topbar-right {
    display: flex;
    gap: 2rem;
    align-items: center;
}

.topbar-left span {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.topbar-left i {
    color: var(--accent-green);
}

.topbar-link {
    color: var(--text-light);
    text-decoration: none;
    transition: color var(--transition-fast);
    padding: 0.25rem 0;
    border-bottom: 2px solid transparent;
}

.topbar-link:hover {
    color: var(--primary-blue-lighter);
    border-bottom-color: var(--primary-blue-lighter);
}


/* ============================================
   HEADER & NAVIGATION
   ============================================ */

header {
    position: fixed;
    top: 37px;
    width: 100%;
    background: linear-gradient(135deg, var(--primary-blue-dark), var(--primary-blue-light));
    backdrop-filter: blur(10px);
    z-index: 1000;
    padding: 0.5rem 0;
    box-shadow: 0 2px 20px rgba(30, 58, 138, 0.3);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    margin-top: 0;
}

nav {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 2rem;
    height: 70px;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.logo-img {
    height: 50px;
    width: auto;
    filter: brightness(0) invert(1);
    transition: transform var(--transition-normal);
}

.logo:hover .logo-img {
    transform: scale(1.05);
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 0.5rem;
    align-items: center;
}

.nav-links li a {
    color: var(--text-light);
    text-decoration: none;
    padding: 0.7rem 1.3rem;
    border-radius: 8px;
    transition: all var(--transition-normal);
    font-weight: 500;
    font-size: 0.95rem;
    position: relative;
    display: inline-block;
}

.nav-links li a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background: white;
    transition: width var(--transition-normal);
}

.nav-links li a:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
}

.nav-links li a:hover::after {
    width: 60%;
}

.admin-btn {
    background: linear-gradient(135deg, var(--accent-red), var(--accent-red-dark)) !important;
    box-shadow: 0 4px 12px rgba(220, 38, 38, 0.2);
}

.admin-btn:hover {
    background: linear-gradient(135deg, var(--accent-red-dark), #991b1b) !important;
    box-shadow: 0 6px 20px rgba(220, 38, 38, 0.4) !important;
}


/* Hamburger Menu */

.hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    padding: 5px;
}

.hamburger span {
    width: 25px;
    height: 3px;
    background: white;
    border-radius: 2px;
    transition: all var(--transition-normal);
}


/* ============================================
   GENERAL SECTION STYLING
   ============================================ */

.section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 2rem 4rem;
    position: relative;
}

.section-content {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    text-align: center;
    padding: 0 1rem;
}

.section-title {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    background: linear-gradient(45deg, var(--primary-blue-light), var(--primary-blue-dark));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: -0.5px;
}

.section-subtitle {
    font-size: 1.25rem;
    color: var(--text-gray);
    margin-bottom: 3rem;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
}


/* ============================================
   HOME SECTION (HERO)
   ============================================ */

#home {
    background: linear-gradient(rgba(15, 23, 42, 0.88), rgba(30, 41, 59, 0.88)), url('https://images.unsplash.com/photo-1560472354-b33ff0c44a43?auto=format&fit=crop&w=1926&q=80');
    background-size: cover;
    background-position: center;